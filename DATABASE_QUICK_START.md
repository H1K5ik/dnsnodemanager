# 🚀 Быстрый старт с базой данных

## 📋 Где создается БД

База данных создается автоматически при первом запуске приложения:
- **Тип**: MySQL
- **Название**: `dnsnodemanager`
- **Пользователь**: `dnsnm_user`
- **Пароль**: `dnsnm123`
- **Конфигурация**: `server/config/database.yml`

## 🛠️ Основные команды

### 1. Создать администратора
```bash
cd server
node create_admin.js
```
**Результат**: Логин `admin`, пароль `admin123`

### 2. Показать информацию о БД
```bash
cd server
node cleanup_database.js 3
```

### 3. Очистить все данные (оставить структуру)
```bash
cd server
node cleanup_database.js 1
```

### 4. Удалить БД полностью
```bash
cd server
node cleanup_database.js 2
```

## 🔄 Полный сброс системы

```bash
# 1. Остановить приложение (Ctrl+C)
# 2. Удалить БД
cd server
node cleanup_database.js 2

# 3. Запустить приложение (создаст БД автоматически)
npm start

# 4. Создать админа
node create_admin.js
```

## 📊 Проверка состояния

```bash
# Показать статистику БД
cd server
node cleanup_database.js 3

# Подключиться к MySQL напрямую
mysql -h localhost -P 3306 -u dnsnm_user -pdnsnm123 dnsnodemanager
```

## ⚠️ Важно

- **Пароль по умолчанию**: `admin123` - измените в продакшене!
- **Резервные копии**: Делайте бэкапы перед очисткой БД
- **Права**: Убедитесь, что MySQL запущен и доступен

## 📝 Структура БД

Основные таблицы:
- `user` - пользователи
- `server` - DNS серверы  
- `zone` - DNS зоны
- `record` - DNS записи
- `ns_group` - группы серверов
- `acl` - списки доступа
- `audit` - журнал действий

Подробная документация: `DATABASE_MANAGEMENT.md`
